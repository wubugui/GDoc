---
id: tutorials-video-to-anim
title: 把视频做成角色动画
sidebar_position: 24
slug: /tutorials/video-to-anim
description: 一段视频抽帧拼图集，挂进动画浏览，在游戏里走起来。
---

# 把视频做成角色动画

雾津街上的人物不能只会站桩——走路、挥手、蹲点查案，都得有**动画**。若你手里是一段拍好的或生成的角色视频，这一页教你变成游戏里能播的精灵动画：抽帧 → 拼图集 → 登记状态 → 预览 → 进场景验证。

---

## 这是什么（30 秒看懂）

把这件事想成把一段皮影戏录像，剪成一张一张定格画片，再钉成一叠能快速翻动的「西洋镜」画册。**视频转图集**这个工具帮你从视频里挑出关键的几帧（比如「走路」的一个完整循环需要哪几张画面）、拼成一张大图（**图集**），再写一份说明书告诉游戏「按什么顺序、多快翻这些画片」。游戏运行时不会真的播放视频，而是按这份说明书快速切换图集里的一格格画面，看起来就像人物在动。

这条管线的核心价值是**手动控帧**：你能精确决定每一帧长什么样、删掉哪些糊的或多余的帧，比全自动生成的动画更可控，适合关键角色、动作分支多的场合。

读完这页你能：

- 用**视频转图集**从视频里抽出动作帧。
- 导出图集与动画描述，供游戏加载。
- 在主编辑器**动画浏览**里核对各状态（站立、行走等）。
- 用**动画预览**和运行预览确认角色动起来，脚点不飘、循环不跳变。

---

## 入门：手把手做第一次

### 整条管线长什么样

```mermaid
flowchart LR
  A[角色动作视频] --> B[视频转图集 · 抽帧拼图]
  B --> C[动画浏览 · 核对状态]
  C --> D[角色登记 · 挂动画包]
  D --> E[场景里看到角色动]
  E --> F[动画预览 / F5 细验]
```

> **图集**：把很多小帧拼成一张大图，游戏按格切出来播——省资源、加载快。

### 第 1 步：素材先入库

视频原始文件建议先按 [导入一张素材](./import-art) 的流程分类导入进工程，方便管理和追溯来源。

### 第 2 步：打开视频转图集

这个工具从主编辑器菜单进（没有单独的 `./dev.sh` 命令）：

```bash
./dev.sh editor
```

菜单栏 → **工具 → 外部工具（新进程）** → 选 **视频转图集**。

也可以 `./dev.sh console`，在工具列表里找同名入口。

### 第 3 步：建工程、抽帧

1. **新建或打开一个工作区**（工具会记住上次进度，工作区数据独立存在一个目录里）。
2. **导入视频**——例如关二狗在码头蹲点张望的片段；一个工作区里可以并存多段视频，各管各的动作。
3. 在时间轴上标出要用的**区间**（哪几秒算「走路」，哪几秒算「站立」）。
4. **预览抽出来的帧**，删掉糊的、重复的、动作跳变的帧——工具支持逐帧微调，帧内容不满意可以直接在这一步剔除。
5. 为每个动作**起名**：常见有 idle（站）、walk（走）、run（跑），命名要和后面场景里要填的状态名保持一致，提前想好命名规范能省很多回头改的功夫。

多段视频可以并进同一个角色工程，各管各的状态；同一个角色不同动作用不同视频拍摄也完全没问题，只要落到同一个工作区。

### 操作示意

<svg viewBox="0 0 700 380" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="视频转图集示意" style={{width:'100%', height:'auto'}}>
  <rect width="700" height="380" fill="#1a1510" rx="8"/>
  <rect x="20" y="20" width="400" height="220" fill="#0d1117" stroke="#3a2f20" rx="6"/>
  <text x="220" y="130" textAnchor="middle" fill="#5a8a86" fontSize="14">视频预览区</text>
  <rect x="20" y="252" width="400" height="48" fill="#231c14" stroke="#3a2f20" rx="4"/>
  <text x="220" y="282" textAnchor="middle" fill="#8a7a5c" fontSize="11">时间轴 · 标记动作区间</text>
  <rect x="440" y="20" width="240" height="340" fill="#1f1810" stroke="#3a2f20" rx="6"/>
  <text x="560" y="48" textAnchor="middle" fill="#c9bda1" fontSize="12">帧列表 / 状态</text>
  <rect x="456" y="60" width="208" height="24" fill="#2a2218" rx="3"/>
  <text x="560" y="76" textAnchor="middle" fill="#f0e7d2" fontSize="10">idle · walk · run</text>
  <rect x="456" y="300" width="208" height="40" fill="none" stroke="#e0a44e" strokeWidth="2" rx="4"/>
  <text x="560" y="326" textAnchor="middle" fill="#e0a44e" fontSize="12">导出图集</text>
</svg>

### 第 4 步：导出

点**导出**，工具会生成：

- 一张**图集大图**（所有帧格排在一起）。
- 一份**动画描述**（哪几格、多快、循不循环）。

导出位置按角色/动画包归好类，方便后面登记。主编辑器里**动画浏览**面板的图集字段是只读的——要换图集，回这个工具重新导。

### 第 5 步：动画浏览里核对

```bash
./dev.sh editor
```

左侧 → **资源与本地化 → 动画浏览**：

1. 找到刚导出的动画包。
2. 看各**状态**的帧序、帧率、是否循环。
3. 世界尺寸不对可在面板里微调（图集本身仍须回视频转图集改）。

想单独、快速地看某个角色某状态，用**动画预览**（渲染规则与游戏完全一致，能看清脚点、循环衔接、滑步等动画浏览小窗看不清的问题）：

```bash
./dev.sh anim-preview
```

浏览器里选角色和状态，所见与游戏里一致。

### 第 6 步：挂到角色、进场景

1. **角色登记**面板 → 选中角色（如关二狗）。
2. 把**动画包**指到刚导出的那份。
3. **场景**面板 → 场景里的 NPC 选同一角色身份，初始状态、巡逻状态填与视频转图集里一致的状态名。
4. **F5** 运行预览，看走路、站立是否顺滑、脚底是否贴地。

脚底飘、比例怪？多半是抽帧区间或世界尺寸要再调一轮。

---

## 雾津完整实例：糖画师傅的招手动画

要给**城隍庙**前卖糖画的师傅补一套「招手揽客」动画：

1. 视频原始素材先按 [导入一张素材](./import-art) 分类导入。
2. 外部工具打开**视频转图集**，导入招手视频，标区间，状态名起 `wave`。
3. 预览抽出的帧，删掉手部动作模糊的那几帧。
4. 导出后进**动画浏览**，确认 `wave` 循环自然，首尾衔接不跳变。
5. **角色登记**里师傅的动画包加上 `wave`。
6. 场景里师傅 NPC 走近能对话；若还要自动招手，在区域或 NPC 行为里编排播放该状态。
7. `./dev.sh anim-preview` 先看大图细验，再 **F5** 进城隍庙验证实际效果。

人动起来，街才活。

---

## 进阶：每一项都讲透

### 工作区与帧管理的进阶用法

- **多段视频并入同一工程**：一个角色的不同动作（走路、站立、特殊动作）完全可以分别拍摄、分别导入，都放进同一个工作区管理，导出时统一打成一个动画包。
- **首尾帧对照预览**：抽帧时工具支持洋葱皮式的对照预览和范围循环播放，方便你在标区间时就发现循环不连贯的问题，而不是等导出后才发现。
- **帧不允许重排、只能尾部追加+中间删除**：每个视频源下的帧管理有这个限制，规划抽帧顺序时要提前想好，不要指望导入完还能任意调整帧的先后顺序。
- **翻转不改原始帧**：需要角色朝另一个方向时，可以对单帧或整个状态做水平翻转，工具只在预览/导出时生成临时翻转副本，不会破坏原始帧数据，比返工重新拍摄省事得多。

### 命名与登记的进阶讲究

- **状态命名要和场景字段严格一致**：视频转图集里起的状态名（如 `idle_dock`、`walk`、`bow`），要跟场景面板里 NPC 的初始状态、巡逻状态字段填的字符串**逐字符一致**，大小写、下划线都要对上，否则游戏里会卡在第一帧或播放默认状态。
- **锚点固定在脚底中心**：所有导出的动画都以脚底中心为锚点，与游戏内精灵实体的对齐方式一致，这也是为什么角色贴地效果依赖这里的抽帧质量。
- **动画浏览的图集字段是只读的**：核对状态没问题后，要修改动画内容（哪怕只是删一帧）都得回视频转图集重新导出，不能在动画浏览面板里直接改。

### 导出与批量技巧

- **分别导出 vs 合并导出**：每个动画可以单独导出成一张小图集，也可以把多个动画合并导出到一张大图集里（每个状态各自独立的描述信息）。合并导出还支持给不同动作clip设置不同的缩放比例，适合给「特写用的慢动作」单独提高精度。
- **每行独立参数**：导出工作台里，添加到导出列表的每一行（每个动画）都可以单独设置缩放、边距、世界尺寸，不是整批统一设置，方便给不同动作定制导出规格。
- **删帧前的引用检查**：工具会在你删除某一帧前，自动扫描所有动画序列里是否还有引用这一帧，并提示确认——这能避免你误删了一个正被某个动画序列使用的关键帧。

### 和其他工具/面板怎么配合

- **动画后处理**：如果你已经有大批量、已经过稳定化处理的视频素材，全自动管线（[动画后处理](../editors/asset-domain/animation-pipeline)）可能比逐帧手动抽帧更省时间；本工具更适合精修关键角色或分支多的动作。
- **动画浏览**：核对状态列表的第一站，发现缺状态时要回视频转图集补一段区间，而不是在场景里硬填一个不存在的状态名。
- **动画预览**：比动画浏览的小窗更适合细验脚点贴地、循环衔接、滑步等问题，与游戏渲染规则完全一致，所见即所得。
- **角色登记**：动画包最终要挂到这里的角色身份上，场景里的 NPC 才能引用到对应动画。

---

## 危险区与边界

- **动画浏览面板里的图集字段是只读的**——主编辑器无法直接修改动画内容，任何调整（哪怕只想删一帧、改一下帧率）都必须回视频转图集或动画后处理重新导出、重新绑定。
- **覆盖旧图集有风险**：场景里已经绑定的动画包，被新的导出结果覆盖后会直接生效——如果有别人正在改这个角色的场景内容，或者你还没确认新动画没问题，先备份旧图集再导出新的。
- **只导出不绑角色 = 白做**：动画浏览能看到新包，但场景里的 NPC 还是要在角色登记里手动指向新包，才会真正生效。
- 更系统的哪里改了会丢，参考 [危险区](../editors/concepts/danger-zone)。

---

## 常见问题

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 角色站立时卡在第一帧 | 场景填的状态名在动画包里不存在 | 回动画浏览核对准确状态名，改场景或补导出 |
| 动画预览能播、游戏里角色不动 | 角色登记未绑最新的动画包 | 回角色登记选中动画包并保存 |
| 走路像在滑步 | 抽帧的步频与场景移动速度不匹配 | 对齐场景移动速度，或换一版步频更合适的抽帧 |
| 循环动作首尾有明显跳变 | 抽帧区间起止点没对齐动作循环点 | 回视频转图集用洋葱皮对照重新标区间 |
| 想改一帧却在动画浏览里找不到入口 | 动画浏览是只读浏览面板 | 回视频转图集里删/换帧后重新导出 |
| 导出后场景里角色变形或比例怪 | 世界尺寸设置和实际角色比例不匹配 | 回视频转图集或动画浏览微调世界尺寸参数 |

---

## 接下来读什么

| 页面 | 内容 |
|---|---|
| [视频转图集工具](../editors/asset-domain/video-to-atlas) | 工具细节 |
| [动画后处理](../editors/asset-domain/animation-pipeline) | 批量自动化产出 |
| [动画预览](../editors/asset-domain/anim-preview) | 快速验动画 |
| [动画浏览面板](../editors/panels/anim-browser) | 核对状态列表 |
| [角色登记面板](../editors/panels/character) | 角色与动画包 |
| [导入一张素材](./import-art) | 静态图入库 |
